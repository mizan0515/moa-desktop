// owned by T12 (v1.5) — body is a placeholder. T12 will render merge order + apply progress.
export default function IntegratePanel() {
  return (
    <section className="results-section">
      <h4>Integrate (v1.5)</h4>
      <p>placeholder — T12 will render merge-order patch apply with conflict halt + Korean report.</p>
    </section>
  );
}
