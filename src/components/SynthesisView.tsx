// owned by T6 — body is a placeholder until T6 wires real synthesis JSON.
export default function SynthesisView() {
  return (
    <section className="results-section">
      <h4>Synthesis (5 columns)</h4>
      <p>placeholder — T6 will render verified / Codex-only / Claude-only / disagreement / open</p>
    </section>
  );
}
